import React from 'react';
import { Word } from '../../data/words';

export const SentenceCompletion: React.FC<{ words: Word[] }> = ({ words }) => {
  return (
    <div>
      <h2>Sentence Completion Game</h2>
      {/* Game implementation will go here */}
    </div>
  );
};